import { ControlLabelPipe } from './control-label.pipe';

describe('ControlLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new ControlLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
